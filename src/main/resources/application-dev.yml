spring:
  datasource:
    url: jdbc:mysql://localhost:3307/finpulse_db?rewriteBatchedStatements=true
    username: ${DB_USERNAME:finpulse}
    password: ${DB_PASSWORD:finpulse123}

  jwt:
    secret: ${JWT_SECRET:this_is_a_dummy_secret_key_32chars_minimum}
  jpa:
    show-sql: true
  sql:
    init:
      mode: always


management:
  endpoint:
    health:
      show-details: always

